import { getStatusBarHeight } from 'react-native-status-bar-height';
import BackgroundImage from './BackgroundImage';
import colors from './colors';
import ViewPropTypes from './ViewPropTypes';
import fonts from './fonts';
import ThemeProvider, { ThemeConsumer, ThemeContext } from './ThemeProvider';
import withTheme from './withTheme';
declare const TextPropTypes: any;
export { BackgroundImage, colors, getStatusBarHeight, ViewPropTypes, TextPropTypes, fonts, ThemeProvider, ThemeConsumer, ThemeContext, withTheme, };
