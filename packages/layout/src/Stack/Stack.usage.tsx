import { usage } from '@rneui/doc-gen';
import { Stack } from './index';
import React from 'react';

usage('', '', ({ Button }) => (
  <Stack row reverse>
    <Button>Hey There</Button>
  </Stack>
));
