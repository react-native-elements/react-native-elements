import { withTheme } from '../config';
import {
  Rating,
  SwipeRatingProps,
} from '@react-native-elements/base/dist/Rating/Rating';

export { Rating };
export type { SwipeRatingProps };
export default withTheme(Rating, 'Rating');
