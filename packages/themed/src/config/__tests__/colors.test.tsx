import { colors, colorsDark } from '..';

describe('Color', () => {
  it.skip('', () => {
    expect(colors).not.toBe(null);
  });

  it.skip('', () => {
    expect(colorsDark).not.toBe(null);
  });
});
